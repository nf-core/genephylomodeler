{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/genephylomodeler/master/assets/schema_input.json",
    "title": "nf-core/genephylomodeler pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "gene_name": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Gene name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "alignment": {
                "type": "string",
                "format": "file-path",
                "pattern": "^\\S+\\.(fna|fasta|fa|fas|nex|nxs|phy|aln)$",
                "errorMessage": "Alignment file must have a valid extension (.fna, .fasta, .fa, .fas, .nex, .nxs, .phy, .aln)"
            },
            "tree": {
                "type": "string",
                "format": "file-path",
                "pattern": "^\\S+\\.(tree|tre|nwk|newick|nex|nxs)$",
                "errorMessage": "Tree file must have a valid extension (.tree, .tre, .nwk, .newick, .nex, .nxs)"
            },
            "suite": {
                "type": "string",
                "enum": ["hyphy"],
                "errorMessage": "Suite must be one of: hyphy"
            },
            "tool": {
                "type": "string",
                "enum": ["absrel", "busted", "fel", "meme", "relax", "slac"],
                "errorMessage": "Tool must be one of: absrel, busted, fel, meme, relax, slac"
            }
        },
        "required": ["gene_name", "alignment", "tree", "suite", "tool"]
    }
}
