{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/phyloanalysis/master/assets/schema_input.json",
    "title": "nf-core/phyloanalysis pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "gene_name": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Gene name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "alignment": {
                "type": "string",
                "format": "file-path",
                "errorMessage": "Alignment file must be provided and cannot contain spaces"
            },
            "tree": {
                "type": "string",
                "format": "file-path",
                "errorMessage": "Tree file must be provided and cannot contain spaces"
            },
            "suite": {
                "type": "string",
                "enum": ["hyphy"],
                "errorMessage": "Suite must be one of: hyphy"
            },
            "tool": {
                "type": "string",
                "enum": ["absrel", "busted", "meme", "relax", "fade"],
                "errorMessage": "Tool must be one of: absrel, busted, meme, relax, fade"
            }
        },
        "required": ["gene_name", "alignment", "tree", "suite", "tool"]
    }
}
